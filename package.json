{
  "name": "wedding-invite",
  "version": "1.0.0",
  "description": "A self-hosted, animated Hindu wedding invitation with config-driven content. RSVPs are collected via a linked Google Form.",
  "private": true,
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "build": "node scripts/build.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
